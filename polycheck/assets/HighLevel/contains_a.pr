def main (w : [Char]) : [Char] := 
    let mut seen_a := False in
    for (i, c) in enumerate(w) do
        if c === 'a' then
            seen_a := True
        endif
    done
    if seen_a then
        return "y"
    else
        return "n"
    endif 
